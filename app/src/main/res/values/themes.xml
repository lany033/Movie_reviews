<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieReviews" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:textViewStyle">@style/TextAppearance.AppCompat.Widget.TextView.SpinnerItem</item>
    </style>

    <style name="Theme.MovieReviews" parent="Base.Theme.MovieReviews" />
    <style name="AppTheme.DarkOverlay" parent="@style/ThemeOverlay.MaterialComponents.Dark.ActionBar" />
</resources>